# Codex Memory

- Project path is `~/Developer/shortcut-index` with singular `Developer`; `~/Developers/shortcut-index` did not exist.
- This folder is not currently a git repository, so track changes by file inspection rather than git status/diff.
- The window `X` is expected to hide the Shortcut Index popover while leaving the tray/background app running; it is not intended to quit the app.
- GUI behavior changes need a restarted Electron process before manual retesting, because an already-open instance can keep using old compiled code.
- Automated checks used here were `npm run lint`, `npm run typecheck`, and `npm run build`; they all passed after the drag-region and close-button fixes.
- I did not launch the foreground Electron app during this session, so actual window drag/click behavior still depends on manual desktop verification.
